<h1><%= @snack.name %><h1>
  <h2>Price: <%= @snack.price %></h2>
  <h2>Location:</h2>
  <ul>
    <% @snack.machines.each do |machine|  %>
    <li> <%= link_to machine.location, machine_path(machine) %> ( <%= machine.count_of %> kinds of snacks, average price of <%= machine.average_price %>)</li>
    <% end %>

  </ul>
